export const NAV = {
    account: "/account",
    cart: "/cart",
    checkout: "/checkout",
    collections: "/collections",
    home: "/",
    signIn: "/sign-in",
    signUp: "/sign-up",
    verify: "/verify",
} as const;

export const AUTH = {
    token: "token",
} as const;

export const ENV = {
    dev: "dev",
    prod: "prod",
    qa: "qa",
} as const;
