export const NAV = {
    home: "/",
    account: "/account",
    signIn: "/sign-in",
    signUp: "/sign-up",
    verify: "/verify",
    cart: "/cart",
    checkout: "/checkout",
    collections: "/collections",
} as const;

export const AUTH = {
    token: "token",
} as const;

export const ENV = {
    dev: "dev",
    qa: "qa",
    prod: "prod",
} as const;
